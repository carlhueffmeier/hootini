// Loosely based on Material Typescale
// https://material.io/design/typography/the-type-system.html#type-scale

const h1 = {
  fontSize: '6rem',
  fontWeight: 300,
  letterSpacing: `${-1.5 / 16}rem`
};

const h2 = {
  fontSize: '3.75rem',
  fontWeight: 300,
  letterSpacing: `${-0.5 / 16}rem`
};

const h3 = {
  fontSize: '3rem',
  fontWeight: 400,
  letterSpacing: `${0 / 16}rem`
};

const h4 = {
  fontSize: '2.125rem',
  fontWeight: 400,
  letterSpacing: `${0.25 / 16}rem`
};

const h5 = {
  fontSize: '1.5rem',
  fontWeight: 400,
  letterSpacing: `${0 / 16}rem`
};

const h6 = {
  fontSize: '1.25rem',
  fontWeight: 400,
  letterSpacing: `${0.15 / 16}rem`
};

const subtitle1 = {
  fontSize: '1rem',
  fontWeight: 400,
  letterSpacing: `${0.15 / 16}rem`
};

const subtitle2 = {
  fontSize: '0.825rem',
  fontWeight: 500,
  letterSpacing: `${0.1 / 16}rem`
};

const body1 = {
  fontSize: '1rem',
  fontWeight: 400,
  letterSpacing: `${0.5 / 16}rem`
};

const body2 = {
  fontSize: '0.825rem',
  fontWeight: 400,
  letterSpacing: `${0.25 / 16}rem`
};

const button = {
  fontSize: '0.825rem',
  fontWeight: 500,
  letterSpacing: `${0.75 / 16}rem`,
  textTransform: 'uppercase',
  whiteSpace: 'nowrap'
};

const caption = {
  fontSize: '0.75rem',
  fontWeight: 400,
  letterSpacing: `${0.4 / 16}rem`
};

const overline = {
  fontSize: '0.6rem',
  fontWeight: 400,
  letterSpacing: `${1.5 / 16}rem`,
  textTransform: 'uppercase'
};

export {
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  subtitle1,
  subtitle2,
  body1,
  body2,
  button,
  caption,
  overline
};
